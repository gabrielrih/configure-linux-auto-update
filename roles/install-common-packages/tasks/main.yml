---
  - name: update
    become: true
    apt:
      update_cache: yes

  - name: install basic packages
    become: true
    package:
      name: "{{ item }}"
      state: latest
    with_items: "{{ packages }}"